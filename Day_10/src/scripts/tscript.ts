let num: number[];

type Custom = string | number;
let c: Custom[] = [1, 2, "sjbjbs"];

interface Person {
  id?: number;
  name: string;
  age: number;
}

const person1: Person = {
  id: 1,
  name: "Boltu",
  age: 20,
};
